# Core that framework uses
fastapi[standard]==0.139.0
uvicorn[standard]==0.49.0
pydantic==2.13.4

# Relational Database (PostgreSQL) & ORM
sqlalchemy==2.0.0
asyncpg==0.30.0        # Async driver for PostgreSQL
alembic==1.14.0        # For database migrations

# Analytical Database (ClickHouse)
clickhouse-connect==0.8.0  # Official driver focused on performance (Bulk Inserts)

# Messaging and Cache (Redis)
redis==5.2.0           # Comes with native async/await support

# Security and Utilities
python-jose[cryptography]==3.3.0  # For generating and validating JWT tokens
passlib[bcrypt]==1.7.4            # For hashing passwords in PostgreSQL

# Linting and Type Checking
mypy==2.1.0
pylint==4.0.6